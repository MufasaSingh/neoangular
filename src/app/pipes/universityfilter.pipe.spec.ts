import { UniversityfilterPipe } from './universityfilter.pipe';

describe('UniversityfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UniversityfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
